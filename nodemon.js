// Global Packages are actaully installed from CLI ( Command Line Interface)
// One famous Example is NODEMON whcih is installed npm install -g Nodemon

// this dependecies actually work as WATCH MODE like whatever changes are done that will be 
// parralley shown quite similaor to the live share 
// To unistall such packages and dependeciends we actaully run commnad adn
// npm uninstall -g nodemon

// where as the local packages are imported from the packages and top like import {react } from react

//nodemon is a tool that helps develop Node.js based applications by automatically restarting the node
// // application when file changes in the directory are detected.

//nodemon does not require any additional changes to your code or method of development.
//  nodemon is a replacement wrapper for node. To use nodemon, replace the word node on the command
//  line when executing your script.