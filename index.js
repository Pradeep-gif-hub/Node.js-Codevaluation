// const superhero=require("/.module_caching");
// console.log(superhero.getName());

// It has got structure as ( _directory,_filename,export,require,caching)

const sum=require("./math");
const val=sum(9,8);
console.log(val);