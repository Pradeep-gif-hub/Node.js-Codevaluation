// it enables the use of thread that executes javascript in parallel 
//the cluster module can run multiple instacnes to distribute the cowrkload
// Working module allows runnign multipme applciation with wuthin single threaded NODE.JS instances
